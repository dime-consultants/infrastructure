---
- name: Remove default Nginx site symlink
  file:
    path: /etc/nginx/sites-enabled/default
    state: absent

- name: Remove default Nginx config file
  file:
    path: /etc/nginx/sites-available/default
    state: absent

- name: Gather all app domains
  set_fact:
    app_domains: >-
      {{
        apps
        | map(attribute='environments')
        | map('dict2items')
        | flatten
        | map(attribute='value.domain')
        | select('defined')
        | select('truthy')
        | list
      }}

- name: Gather all extra domains
  set_fact:
    extra_only_domains: >-
      {{
        extra_domains
        | map(attribute='domain')
        | difference(app_domains)
        | list
      }}

- name: Combine all domains
  set_fact:
    all_domains: "{{ (app_domains + extra_only_domains) | unique }}"

# -------------------------
# HTTP CONFIGS ONLY
# -------------------------

- name: Generate HTTP configs for apps
  include_tasks: generate-nginx-app.yml
  loop: "{{ apps }}"
  loop_control:
    loop_var: app
  vars:
    ssl_enabled: false

- name: Generate HTTP configs for extra tools
  template:
    src: ../templates/nginx-extra-http.j2
    dest: "/etc/nginx/sites-available/extra-{{ item.domain | replace('.', '-') }}.conf"
    mode: '0644'
  loop: "{{ extra_domains }}"
  when: item.domain in extra_only_domains

- name: Enable HTTP sites
  file:
    src: "/etc/nginx/sites-available/{{ item }}"
    dest: "/etc/nginx/sites-enabled/{{ item }}"
    state: link
  loop: "{{ lookup('ansible.builtin.fileglob', '/etc/nginx/sites-available/*.conf', wantlist=True) | map('basename') | list }}"

- name: Test nginx HTTP config
  command: nginx -t
  changed_when: false

- name: Reload nginx (HTTP)
  service:
    name: nginx
    state: reloaded

# -------------------------
# CERTBOT
# -------------------------

- name: Obtain certificates via webroot
  command: >
    certbot certonly
    --webroot
    -w /var/www/letsencrypt
    -d {{ all_domains | join(' -d ') }}
    --non-interactive
    --agree-tos
    --email admin@mchangohub.com
  when: all_domains | length > 0
  register: certbot
  changed_when: "'Congratulations' in certbot.stdout"

# -------------------------
# HTTPS CONFIGS
# -------------------------

- name: Regenerate HTTPS configs for apps
  include_tasks: generate-nginx-app.yml
  loop: "{{ apps }}"
  loop_control:
    loop_var: app
  vars:
    ssl_enabled: true

- name: Generate HTTPS configs for extra tools
  template:
    src: ../templates/nginx-extra-https.j2
    dest: "/etc/nginx/sites-available/extra-{{ item.domain | replace('.', '-') }}.conf"
    mode: '0644'
  loop: "{{ extra_domains }}"
  when: item.domain in extra_only_domains

- name: Test nginx HTTPS config
  command: nginx -t
  changed_when: false

- name: Reload nginx (HTTPS)
  service:
    name: nginx
    state: reloaded
