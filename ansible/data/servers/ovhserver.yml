server_name: ovhserver
ssh_user: ubuntu
ssh_secret_name: OVHSERVER_SSH_KEY
public_ip: 51.77.147.233

firewall_extra_ports: []

apps:
  - name: zanaka
    repo: SteveNdegwa/zanaka
    environments:
      prod:
        domain: app.mchangohub.com
        port: 8000
  
#  - name: zanaka-ui
#    repo: SteveNdegwa/zanaka-ui
#    environments:
#      prod:
#        domain: api.mchangohub.com
#        port: 4000


# Additional internal/admin tools exposed via subdomains
# These are NOT full apps with their own repo/deploy — just reverse proxies
extra_domains:
   - domain: flower.mchangohub.com
     proxy_port: 5555
   - domain: portainer.mchangohub.com
     proxy_port: 9000
   - domain: notifications.mchangohub.com
     proxy_port: 3000
   - domain: rabbit.mchangohub.com
     proxy_port: 15672
     basic_auth: true